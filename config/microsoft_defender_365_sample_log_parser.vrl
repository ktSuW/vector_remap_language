# Parse the JSON message
. = parse_json!(.message)

.original_message = encode_json(.)

.alert_id = .id
.tenant_id = .tenantId
#.alert_title = alertInfo.title
#.alert_severity = .alertInfo.severity
#.alert_status = .alertInfo.status

.device_name = .entities.devices[0].info.name
.user_name = .entities.users[0].accountName