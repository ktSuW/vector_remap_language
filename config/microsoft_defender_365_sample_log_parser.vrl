.puuid = uuid_v4()
.original_event = parse_json!(.message)

.devices_array = .original_event.entities.devices
.devices_platform = []

.devices_platform = map_values(array(.devices_array) ?? []) -> |device| {
  device.info.os.platform
}

del(.original_event)
del(.message)